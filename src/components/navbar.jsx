import React from 'react'
import { Layout } from 'antd'
const { Header } = Layout

const Navbar = () => {
  return (
    <Header style={{height:'50px', display:'flex', justifyContent:'center', lineHeight:'normal', alignItems:'center'}}>navbar</Header>
  )
}

export default Navbar